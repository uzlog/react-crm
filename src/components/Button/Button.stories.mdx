import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { withKnobs, boolean } from "@storybook/addon-knobs";
import { Button } from "./Button";
import { buttonSizes } from "../../constants/button";

<Meta title="Button" component={Button} decorators={[withKnobs]} />

# Button sizes

<Preview>
    <Story name="small">
        <Button size={buttonSizes.small}>A Button</Button>
    </Story>
    <Story name="normal">
        <Button size={buttonSizes.normal}>A Button</Button>
    </Story>
    <Story name="large">
        <Button size={buttonSizes.large}>A Button</Button>
    </Story>
    <Story name="full width">
        <Button size={buttonSizes.large} fullWidth>
            A Button
        </Button>
    </Story>
</Preview>

# Other styles

<Preview>
    <Story name="loading">
        <Button isLoading={boolean("isLoading", true)}>A Loading Button</Button>
    </Story>
</Preview>

<Preview>
    <Story name="disabled">
        <Button disabled>A Disabled Button</Button>
    </Story>
</Preview>

## Props

<Props of={Button} />
